# Infrastructure Intelligence Platform - Python Dependencies
# Install with: pip install -r requirements.txt
# Note: gdal and osmium-tool should be installed via conda (see environment.yml)

# Core geospatial I/O
# NOTE: rasterio is pinned to <1.5 because the 1.5.x Windows wheel carries an
# unsigned native DLL that Windows Application Control blocks on some machines.
rasterio>=1.3.0,<1.5
shapely>=2.0.0
geopandas>=0.14.0
pyproj>=3.6.0
fiona>=1.9.0

# Least-cost path and spatial algorithms
scikit-image>=0.21.0
scipy>=1.11.0

# Array and tabular processing
numpy>=1.24.0
pandas>=2.0.0

# OSM PBF parsing
osmium>=4.3.0

# Raster zonal statistics
rasterstats>=0.19.0

# API and Configuration
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.24.0
pyyaml>=6.0
requests>=2.31.0
tqdm>=4.66.0

# Authentication - Supabase JWT validation (server-side only)
# PyJWT validates RS256-signed Supabase JWTs; cryptography provides RSA key support.
PyJWT>=2.8.0
cryptography>=41.0.0
# Official Supabase Python client for server-side DB operations (service-role key only)
supabase>=2.3.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
